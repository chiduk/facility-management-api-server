import * as deviceTokenInterface from './deviceToken.interfaces';
import * as deviceTokenService from './deviceToken.service';
import DeviceToken from './deviceToken.model';

export { deviceTokenInterface, deviceTokenService, DeviceToken };
